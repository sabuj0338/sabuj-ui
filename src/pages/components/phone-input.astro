---
import { PhoneInput as UIPhoneInput } from "@/components/ui/phone-input"
import Layout from "@/layouts/main.astro"
import SiteHeader from "@/components/site-header.astro"
import phoneInputCode from "@/components/ui/phone-input.tsx?raw"
import {
  Copy,
  Download,
  FileCode2,
  FileText,
  Globe2,
  PanelLeft,
  Phone,
  Terminal,
} from "lucide-react"

const component = {
  id: "phone-input",
  name: "Phone Input",
  status: "Ready",
  description:
    "An international phone number input with country search, dialing codes, flags, and shadcn-styled controls.",
  file: "src/components/ui/phone-input.tsx",
  source: phoneInputCode,
  exports: ["PhoneInput"],
  dependencies: [
    "react-phone-number-input",
    "lucide-react",
    "@/components/ui/button",
    "@/components/ui/command",
    "@/components/ui/input",
    "@/components/ui/popover",
    "@/components/ui/scroll-area",
    "@/lib/utils",
  ],
  props: [
    {
      name: "value",
      type: "RPNInput.Value",
      defaultValue: "-",
      description:
        "The controlled E.164 phone number value passed through react-phone-number-input.",
    },
    {
      name: "onChange",
      type: "(value: RPNInput.Value) => void",
      defaultValue: "-",
      description:
        "Called whenever the number changes. Empty or invalid values are normalized to an empty string.",
    },
    {
      name: "defaultCountry",
      type: "RPNInput.Country",
      defaultValue: "-",
      description:
        "Sets the initial country for formatting and calling-code selection.",
    },
    {
      name: "international",
      type: "boolean",
      defaultValue: "-",
      description:
        "Uses international formatting behavior from react-phone-number-input.",
    },
    {
      name: "placeholder",
      type: "string",
      defaultValue: "-",
      description: "Placeholder text for the phone number input.",
    },
    {
      name: "disabled",
      type: "boolean",
      defaultValue: "false",
      description: "Disables the country selector and input field.",
    },
    {
      name: "className",
      type: "string",
      defaultValue: "-",
      description:
        "Extends the wrapper class list through the local cn helper.",
    },
  ],
}

const noop = () => undefined

const phoneInputExample = String.raw`import { PhoneInput } from "@/components/ui/phone-input"

export function ContactPhoneField() {
  const [phone, setPhone] = React.useState("")

  return (
    <PhoneInput
      defaultCountry="US"
      international
      value={phone}
      onChange={setPhone}
      placeholder="Enter phone number"
    />
  )
}`
---

<Layout
  title="Phone Input Component - Sabuj UI"
  description="An international phone number input with country search, dialing codes, flags, and shadcn-styled controls."
  keywords="phone input component, react phone number input, international phone input, country selector, shadcn phone input, tailwind phone input"
  type="article"
>
  <div class="min-h-screen bg-background text-foreground">
    <SiteHeader active="phone-input" />

    <div
      id="components"
      class="mx-auto grid max-w-7xl gap-8 px-5 pt-24 lg:grid-cols-[17rem_minmax(0,1fr)] lg:px-8"
    >
      <aside class="lg:sticky lg:top-20 lg:h-[calc(100vh-6rem)]">
        <nav aria-label="Documentation navigation" class="space-y-8 text-sm">
          <div>
            <div class="mb-3 flex items-center gap-2 font-semibold">
              <PanelLeft className="size-4 text-primary" />
              Components
            </div>
            <div class="flex flex-col gap-1 border-l-2 border-border/50 pl-3">
              <a
                href="/#components"
                class="rounded-md px-2 py-1.5 text-muted-foreground transition-colors hover:text-foreground"
              >
                Overview
              </a>
              <a
                href="/components/timeline/"
                class="rounded-md px-2 py-1.5 text-muted-foreground transition-colors hover:text-foreground"
              >
                Timeline
              </a>
              <a
                href="/components/paginator/"
                class="rounded-md px-2 py-1.5 text-muted-foreground transition-colors hover:text-foreground"
              >
                Paginator
              </a>
              <a
                href="/components/phone-input/"
                class="rounded-md bg-accent px-2 py-1.5 font-medium text-foreground transition-colors"
              >
                Phone Input
              </a>
              <a
                href="/docs/component-documentation-guide/"
                class="rounded-md px-2 py-1.5 text-muted-foreground transition-colors hover:text-foreground"
              >
                Guide
              </a>
            </div>
          </div>

          <div class="hidden lg:block">
            <p class="mb-3 font-semibold">On this page</p>
            <div class="flex flex-col gap-1 border-l-2 border-border/50 pl-3">
              <a
                href="#preview"
                class="block rounded-md px-2 py-1.5 text-muted-foreground hover:text-foreground"
              >
                Preview
              </a>
              <a
                href="#usage"
                class="block rounded-md px-2 py-1.5 text-muted-foreground hover:text-foreground"
              >
                Usage
              </a>
              <a
                href="#api"
                class="block rounded-md px-2 py-1.5 text-muted-foreground hover:text-foreground"
              >
                API reference
              </a>
            </div>
          </div>
        </nav>
      </aside>

      <main class="min-w-0">
        <section class="mb-8 space-y-4">
          <div class="flex flex-wrap items-center gap-2">
            <span
              class="inline-flex items-center rounded-md bg-primary/10 px-2 py-1 text-xs font-medium text-primary ring-1 ring-primary/20"
            >
              {component.status}
            </span>
            <code
              class="rounded-md bg-muted px-2 py-1 text-xs text-muted-foreground"
            >
              {component.file}
            </code>
          </div>
          <div class="space-y-3">
            <p class="text-sm font-medium text-primary">Component</p>
            <h1 class="text-4xl font-bold tracking-tight">{component.name}</h1>
            <p class="max-w-2xl text-base leading-7 text-muted-foreground">
              {component.description}
            </p>
          </div>
        </section>

        <section class="rounded-lg border bg-background shadow-sm">
          <div class="min-w-0 divide-y">
            <section id="preview" class="scroll-mt-24 p-5 md:p-6">
              <div
                class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"
              >
                <div
                  class="inline-flex w-fit items-center rounded-md border bg-muted p-1 text-sm font-medium"
                  role="tablist"
                >
                  <button
                    type="button"
                    class="rounded-sm bg-background px-3 py-1.5 shadow-sm transition-all focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
                    role="tab"
                    aria-selected="true"
                    data-tab-group="phone-input"
                    data-tab-target="phone-input-preview-content"
                  >
                    Preview
                  </button>
                  <button
                    type="button"
                    class="rounded-sm px-3 py-1.5 text-muted-foreground transition-all hover:text-foreground focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
                    role="tab"
                    aria-selected="false"
                    data-tab-group="phone-input"
                    data-tab-target="phone-input-code-content"
                  >
                    Code
                  </button>
                </div>

                <div class="flex items-center gap-2">
                  <button
                    type="button"
                    class="inline-flex h-8 items-center justify-center gap-2 rounded-md border bg-background px-3 text-xs font-medium transition-colors hover:bg-accent"
                    data-copy-target="phone-input-source"
                  >
                    <Copy className="size-3.5" />
                    <span data-button-label>Copy</span>
                  </button>
                  <button
                    type="button"
                    class="inline-flex h-8 items-center justify-center gap-2 rounded-md border bg-background px-3 text-xs font-medium transition-colors hover:bg-accent"
                    data-download-target="phone-input-source"
                    data-download-file="phone-input.tsx"
                  >
                    <Download className="size-3.5" />
                    <span data-button-label>Download</span>
                  </button>
                </div>
              </div>

              <div id="phone-input-preview-content" role="tabpanel">
                <div class="overflow-hidden rounded-lg border bg-muted/20">
                  <div
                    class="grid gap-6 bg-background/86 p-5 md:grid-cols-2 md:p-8"
                  >
                    <div class="space-y-5">
                      <div>
                        <div
                          class="mb-2 flex items-center gap-2 text-sm font-semibold"
                        >
                          <Phone className="size-4 text-primary" />
                          Primary phone
                        </div>
                        <UIPhoneInput
                          defaultCountry="US"
                          international
                          value="+14155552671"
                          onChange={noop}
                          placeholder="Enter phone number"
                        />
                      </div>

                      <div>
                        <div
                          class="mb-2 flex items-center gap-2 text-sm font-semibold"
                        >
                          <Globe2 className="size-4 text-primary" />
                          Support line
                        </div>
                        <UIPhoneInput
                          defaultCountry="BD"
                          international
                          value="+8801712345678"
                          onChange={noop}
                          placeholder="Enter phone number"
                        />
                      </div>
                    </div>

                    <div class="rounded-lg border bg-background p-4">
                      <p class="text-sm font-semibold">
                        Country-aware formatting
                      </p>
                      <p class="mt-2 text-sm leading-6 text-muted-foreground">
                        The selector uses a searchable command menu, calling
                        codes, and flag assets from <code
                          >react-phone-number-input</code
                        >.
                      </p>
                      <div class="mt-5 space-y-3 text-sm">
                        <div class="flex items-center justify-between gap-4">
                          <span class="text-muted-foreground">Format</span>
                          <code class="rounded-md bg-muted px-2 py-1">
                            E.164
                          </code>
                        </div>
                        <div class="flex items-center justify-between gap-4">
                          <span class="text-muted-foreground">Default UI</span>
                          <span class="font-medium">Dial code + input</span>
                        </div>
                        <div class="flex items-center justify-between gap-4">
                          <span class="text-muted-foreground">Search</span>
                          <span class="font-medium">Country name</span>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <div id="phone-input-code-content" role="tabpanel" class="hidden">
                <pre
                  class="max-h-152 overflow-auto rounded-lg border bg-zinc-950 p-4 text-sm leading-6 text-zinc-100"><code id="phone-input-source">{component.source}</code></pre>
              </div>

              <div class="mt-6 rounded-lg border bg-muted/20 p-5 md:p-6">
                <div class="grid gap-6 sm:grid-cols-3">
                  <div>
                    <h3 class="mb-3 text-sm font-semibold">Exports</h3>
                    <div class="flex flex-wrap gap-2">
                      {
                        component.exports.map((item) => (
                          <code class="rounded-md border bg-background px-2 py-1 text-xs">
                            {item}
                          </code>
                        ))
                      }
                    </div>
                  </div>
                  <div>
                    <h3 class="mb-3 text-sm font-semibold">Dependencies</h3>
                    <div class="flex flex-wrap gap-2">
                      {
                        component.dependencies.map((item) => (
                          <div class="rounded-md border bg-background px-2 py-1 text-xs">
                            <code>{item}</code>
                          </div>
                        ))
                      }
                    </div>
                  </div>
                  <div>
                    <h3 class="mb-3 text-sm font-semibold">Install notes</h3>
                    <p class="text-xs leading-5 text-muted-foreground">
                      Requires <code>react-phone-number-input</code>, its flag
                      data, and the local shadcn-style command, popover, scroll
                      area, button, and input primitives.
                    </p>
                  </div>
                </div>
              </div>
            </section>

            <section id="usage" class="scroll-mt-24 p-5 md:p-6">
              <div class="mb-4 flex items-center gap-2 text-sm font-semibold">
                <Terminal className="size-4 text-primary" />
                Usage
              </div>
              <pre
                class="overflow-auto rounded-lg border bg-muted p-4 text-sm leading-6"><code>{phoneInputExample}</code></pre>
            </section>

            <section id="api" class="scroll-mt-24 p-5 md:p-6">
              <div class="mb-4 flex items-center gap-2 text-sm font-semibold">
                <FileCode2 className="size-4 text-primary" />
                API reference
              </div>
              <div class="overflow-x-auto rounded-lg border">
                <table class="w-full min-w-2xl text-left text-sm">
                  <thead
                    class="border-b bg-muted/60 text-xs text-muted-foreground"
                  >
                    <tr>
                      <th class="px-4 py-3 font-medium">Prop</th>
                      <th class="px-4 py-3 font-medium">Type</th>
                      <th class="px-4 py-3 font-medium">Default</th>
                      <th class="px-4 py-3 font-medium">Description</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y">
                    {
                      component.props.map((prop) => (
                        <tr>
                          <td class="px-4 py-3 align-top">
                            <code class="font-semibold">{prop.name}</code>
                          </td>
                          <td class="px-4 py-3 align-top text-muted-foreground">
                            <code>{prop.type}</code>
                          </td>
                          <td class="px-4 py-3 align-top text-muted-foreground">
                            <code>{prop.defaultValue}</code>
                          </td>
                          <td class="px-4 py-3 align-top leading-6">
                            {prop.description}
                          </td>
                        </tr>
                      ))
                    }
                  </tbody>
                </table>
              </div>
            </section>

            <section class="border-t px-6 py-10">
              <div
                class="grid gap-6 lg:grid-cols-[minmax(0,1fr)_18rem] lg:items-start"
              >
                <div class="space-y-4">
                  <p class="text-sm font-medium text-primary">Pattern</p>
                  <h2 class="text-3xl font-semibold">
                    Store normalized phone values
                  </h2>
                  <p class="max-w-2xl text-sm leading-6 text-muted-foreground">
                    Use the E.164 value from <code>onChange</code> in form state,
                    then validate or submit it with the rest of your contact fields.
                  </p>
                  <a
                    href="/docs/component-documentation-guide/"
                    class="inline-flex h-10 items-center justify-center gap-2 rounded-md border bg-background px-4 text-sm font-medium transition-colors hover:bg-accent"
                  >
                    <FileText className="size-4" />
                    Open guide file
                  </a>
                </div>

                <div class="rounded-lg border bg-background p-4 shadow-sm">
                  <p class="text-sm font-semibold">Form usage</p>
                  <p class="mt-2 text-sm leading-6 text-muted-foreground">
                    Works well for account settings, checkout forms, booking
                    flows, and support contact details.
                  </p>
                </div>
              </div>
            </section>
          </div>
        </section>
      </main>
    </div>
  </div>

  <script>
    const copyButtons = document.querySelectorAll("[data-copy-target]")
    const downloadButtons = document.querySelectorAll("[data-download-target]")
    const tabButtons = document.querySelectorAll("[data-tab-target]")

    copyButtons.forEach((button) => {
      button.addEventListener("click", async () => {
        const targetId = button.getAttribute("data-copy-target")
        const target = targetId ? document.getElementById(targetId) : null
        const code = target?.textContent ?? ""
        const label = button.querySelector("[data-button-label]")
        const originalText = label?.textContent ?? "Copy"

        await navigator.clipboard.writeText(code)
        if (label) label.textContent = "Copied"

        window.setTimeout(() => {
          if (label) label.textContent = originalText
        }, 1400)
      })
    })

    downloadButtons.forEach((button) => {
      button.addEventListener("click", () => {
        const targetId = button.getAttribute("data-download-target")
        const fileName =
          button.getAttribute("data-download-file") ?? "component.tsx"
        const target = targetId ? document.getElementById(targetId) : null
        const code = target?.textContent ?? ""
        const blob = new Blob([code], { type: "text/plain;charset=utf-8" })
        const url = URL.createObjectURL(blob)
        const link = document.createElement("a")

        link.href = url
        link.download = fileName
        link.click()
        URL.revokeObjectURL(url)
      })
    })

    tabButtons.forEach((button) => {
      button.addEventListener("click", () => {
        const group = button.getAttribute("data-tab-group")
        const targetId = button.getAttribute("data-tab-target")

        document
          .querySelectorAll(`[data-tab-group="${group}"]`)
          .forEach((btn) => {
            btn.setAttribute("aria-selected", "false")
            btn.classList.remove(
              "bg-background",
              "shadow-sm",
              "text-foreground"
            )
            btn.classList.add("text-muted-foreground")
          })

        button.setAttribute("aria-selected", "true")
        button.classList.add("bg-background", "shadow-sm", "text-foreground")
        button.classList.remove("text-muted-foreground")

        const section = button.closest("section")
        if (section) {
          section.querySelectorAll('[role="tabpanel"]').forEach((panel) => {
            panel.classList.add("hidden")
          })
          const targetPanel = document.getElementById(targetId || "")
          if (targetPanel) {
            targetPanel.classList.remove("hidden")
          }
        }
      })
    })
  </script>
</Layout>
